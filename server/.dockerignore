# ============================================================================
# Docker 构建上下文忽略清单（源码构建 targetPath=server/ 时生效）
# ----------------------------------------------------------------------------
# 只影响「上传给构建机的上下文」，不影响运行时行为。
#
# 真码依据（Dockerfile）：镜像只 COPY 这几项 ——
#   requirements.txt / app.py / mcp_server.py / engine / knowledge / data / static
# 其余文件从来不进镜像；放进本清单的唯一收益是**少传构建上下文**。
#
# 实测收益：构建上下文 149.3 MB → 0.95 MB。
#   被排掉 8526 个文件 / 148.44 MB；只剩 30 个文件 / 0.95 MB（即真正要构建的东西）。
#   最大一块是三个本地虚拟环境 —— .venv 65.8 + .venv_fresh 18.3 +
#   .venv_repro_1789995829 64.2 = 148.3 MB / 8503 文件，而它们在 Dockerfile 里
#   连 COPY 都没出现，纯属白传。
#
# ⚠️ 上述数字是按本清单逐条试算得出的（本机无 docker，未跑真实 build 核对）；
#    语义按 Docker 的 MatchesOrParentMatches（命中祖先目录即整棵排除）实现。
# ============================================================================

# —— Python 缓存 ——
__pycache__/
*.pyc

# —— 本地虚拟环境 ——
# 构建机上由 pip install -r requirements.txt 重建，一律不上传。
# 通配覆盖 .venv / .venv_fresh / .venv_repro_* 等所有变体。
# 用 `.venv*`（不带尾斜杠）：单段模式在任意层级生效、且同时匹配文件与目录，
# 命中目录即整棵排除 —— 不依赖「尾斜杠 + 通配」的组合语义。
.venv*

# —— VCS / 系统杂项 ——
.git/
.DS_Store

# —— 本地探针与验证脚本 / 临时输出（只在开发机上跑）——
# ⚠️ 前导 "/" 必须保留：它把通配锚定到构建根，只排 server/ 根下的脚本。
#    若删掉斜杠写成 `_*.py`，会连 engine/__init__.py 一并排掉，镜像直接坏。
/_*.py
/*_out.txt

# —— 日志（server/ 根与 data/ 下的运行时日志都被这条覆盖）——
*.log

# —— 上传的临时图谱 / 备份（运行时产生，无需入镜像）——
data/_up_*.json
data/*.bak
data/_verify_*.py

# —— 引擎镜像树：构建只需要源码，本地构建产物不入镜像 ——
engine/build/
