# 屋顶/楼板生成机制分析：为何模型看起来像一个封闭大盒子

> 结论先行：**屋顶与空间名称有关**，但真正的根因是**编码不匹配** —— 建模库用 GBK 字面量
> 去比对 UTF-8 传入的空间名，**永远匹配不上**，导致包括「庭院」在内的**全部空间**都被
> 判定为室内空间，从而统统盖上屋顶与楼板，把整个院落封成一个盒子。
>
> 当前采取的策略：**不改引擎**，在前端按「水平面面积占比」动态剔除楼板/屋顶大平板，
> 露出内部墙体与门窗结构。

---

## 1. 判定规则的源码位置

`n1/modelling/site.cpp` L1519–1527：

```cpp
void meta_impl::Space::_match_space_type()
{
    if (_name.contains("阳台"))        // 源文件为 GBK 编码
        _spaceType = emBalcony;
    else if (_name.contains("院"))     // 源文件为 GBK 编码
        _spaceType = emCourtyard;
    else
        _spaceType = emInnerSpace;     // 兜底：一律视为室内
}
```

调用点：`Space::setName()`（L1374–1380），即每次设置空间名时自动判定类型。

## 2. 屋顶/楼板如何依类型生成

`n1/modelling/site.cpp` `Storey::modelling()` L2442–2468：

```cpp
for (每个空间) {
    if (space.isUnbounded()) continue;

    // 楼板：非庭院空间都建地板
    if (spaceType != Space::emCourtyard) {
        space.floor().modelling(body0);
        shapes.push_back(body0);
    }

    // 屋顶：室内空间建屋顶
    if (spaceType == Space::emInnerSpace) {
        space.roof().modelling(body1);
        shapes.push_back(body1);
    }
}
```

- 板厚：`Slab::_thickness = 300`（L306）
- 结论：**只有 `emCourtyard`（庭院）不建地板、`emBalcony`/`emCourtyard` 不建屋顶**；
  其余一律「地板 + 屋顶」齐全。

## 3. 编码不匹配的实证

源文件 `site.cpp` 为 **GBK 编码**，字面量字节：

| 语义 | 编码 | 字节 |
|---|---|---|
| 阳台 | GBK | `D1 F4 CC A8` |
| 院 | GBK | `D4 BA` |

图谱 JSON 为 **UTF-8**，空间名字节：

| 空间名 | UTF-8 字节 |
|---|---|
| 庭院 | `E5 BA AD E9 99 A2` |
| 正房 | `E6 AD A3 E6 88 BF` |
| 倒座房 | `E5 80 92 E5 BA A7 E6 88 BF` |
| 东厢房 | `E4 B8 9C E5 8E A2 E6 88 BF` |
| 西厢房 | `E8 A5 BF E5 8E A2 E6 88 BF` |
| 大门 | `E5 A4 A7 E9 97 A8` |
| 厕所 | `E5 8E 95 E6 89 80` |

`D4 BA`（GBK 的「院」）**不出现在任何 UTF-8 字节串中** → `contains` 恒为 false
→ 即使「庭院」也会落到 `else` 分支 → 判定为 `emInnerSpace`。

### 实测判定结果（7 个空间全部为室内）

| 空间名 | 实际判定 |
|---|---|
| 庭院 | `emInnerSpace`（**错误，应为 emCourtyard**） |
| 正房 | `emInnerSpace` |
| 倒座房 | `emInnerSpace` |
| 东厢房 | `emInnerSpace` |
| 西厢房 | `emInnerSpace` |
| 大门 | `emInnerSpace` |
| 厕所 | `emInnerSpace` |

## 4. STL 几何佐证：四层大板盖满全院

对 `shapes_case6.stl` 中法向 ±Z 的三角面按标高聚合（面积单位 m²）：

| z (mm) | 面积 | 性质 |
|---|---|---|
| 0 | 997 | 楼板底面 |
| 300 | 926 | 楼板顶面（板厚 300） |
| 2800 | 0.27 | 门洞顶面（**应保留**） |
| 3200 | 926 | 屋顶底面（层高 3500 − 300） |
| 3500 | 997 | 屋顶顶面 |

院落外轮廓面积 893 m²（25.2 m × 35.3 m）。四层板（0/300/3200/3500）**每层均盖满整个外轮廓**
→ 从外部观察即一个严丝合缝的封闭盒子，内部墙体与门窗洞全部被遮挡。

## 5. 当前处理：前端动态剔除楼板/屋顶

实现位置：`server/static/js/main.js` 的 `parseSTLText()`（两遍扫描）

1. **第一遍**：收集全部三角面，按 `round(z)` 聚合「水平面（|nz| > 0.9）」的总面积，
   并求出模型外轮廓面积。
2. **判定**：某标高的水平面总面积 > 外轮廓面积 × 0.5 → 记入待剔除集合 `dropZ`。
3. **第二遍**：输出时跳过「水平且 z 命中 dropZ」的面；其余全部保留。

选择此方案的理由：

- 不动建模引擎，零编译风险；
- 按「面积占比」动态判定而非写死标高，换其他图谱可自适应；
- 门/窗洞顶面面积极小（0.27 m²），天然不会被误剔。

### 实测结果

```
外轮廓面积 = 893.1 m²
各水平层面积：
   z=    0    997.19 m²  <== 剔除
   z=  300    926.33 m²  <== 剔除
   z= 2800      0.27 m²  保留      ← 门洞顶面
   z= 3200    926.33 m²  <== 剔除
   z= 3500    997.46 m²  <== 剔除

总面 588 -> 保留 382，剔除 206
```

## 6. 后续可选方向（暂不实施）

| 方案 | 做法 | 代价 |
|---|---|---|
| 修正引擎编码 | 将 `site.cpp` 字面量改为 UTF-8，或改用与编码无关的判定方式 | 需重新编译建模库；且修后仅「庭院」识别正确，其余房间**按语义仍应盖屋顶** |
| 引擎侧开关 | 在 `Storey::modelling` 增加「是否生成楼板/屋顶」参数 | 需改 C++ 接口并重编 |
| 前端开关 | 增加「显示屋顶 / 只看结构」切换按钮 | 纯前端改动，最轻 |

> 注：即使修正编码，「正房 / 倒座房 / 东厢房 / 西厢房 / 大门 / 厕所」**仍会被判定为室内并盖屋顶**
> —— 这符合建筑语义。若要完整展示内部结构，引擎侧的开关或前端过滤仍是必要手段。
