# 架构设计总览（思路存档）

> 本文为 building-wiki 项目架构讨论的收敛存档，记录经多轮澄清后锁定的框架、命名与数据流。
> 定位：类型图谱(WHAT) + 实例图谱(数据) + 规则库(WHERE) + 几何计算引擎(纯业务几何计算·算位置) + 几何造型引擎(据结果以 BOX/BREP 表达)；以「数据驱动」为底座，知识中心驱动生成、数据中心积累反哺。
> 旧文档 `MVP实施文档.md` 的 §4 schema v0（含 `pos{x,y,z}`/`axisAlign`）已违反本存档的「零坐标红线」，待后续同步修订。

---

## 0. 一句话定位

类型图谱定义「是什么」、实例图谱填「具体数据」，规则库定义「放哪儿」；几何计算引擎做纯业务几何计算（算相对位置/布局，与体素无关），几何造型引擎据其结果决定用体素 BOX 还是 BREP 表达；知识中心在生成时主动驱动，数据中心在生成外沉淀与反哺。

---

## 1. 命名锁定（按最新框架修订）

> 早先锁定的「BRG 图谱 / 规则库 / 几何计算引擎 三名字」已按最新框架修订：图谱裂为**类型图谱 + 实例图谱**两层，并新拆出**几何造型引擎**。规则库不更名。

五大命名与逐一定位：
- **类型图谱（WHAT·知识）**：定义构件+角色+组合关系+比例模数（零坐标、无绝对米数）。
- **实例图谱（数据）**：继承类型结构、填具体长宽进深、可偏离标准型。
- **规则库（WHERE·放哪儿）**：方位 / 朝向 / 围合约束。
- **几何计算引擎（怎么算）**：吃 类型图谱 + 实例 + 规则 → 输出**相对位置清单**（纯业务几何计算，只算相对位置/布局，不出形状，与体素无关）。
- **几何造型引擎（新）**：吃位置清单 → 输出最终几何；**两个出口**：
  - BOX 体素（MVP 必做）
  - BREP 真实几何（OCC，延后）

> 关键纠错：BOX 与 BREP 都是几何**造型**引擎的产出（两种保真度），几何**计算**引擎只算位置、不出形状。

---

## 2. 两层模型（实例 vs 类型）

- **类型图谱（知识 · 知识中心）** = 参数化模板 / class：
  - 只含结构骨架、角色、组合关系、比例模数（如 明间:次间 ≈ 10:8）。
  - **无绝对米数、无方位**。
- **实例图谱（数据 · 数据中心）** = 套具体参数的 instance：
  - 继承类型结构、填长宽进深、可偏离标准型。

类比：类型图谱 ≈ CAD 参数化模型，实例图谱 ≈ 实例化模型。但本项目的类型图谱装的是**结构/语义参数**，几何求解交由引擎完成（非几何参数化）。

---

## 3. 两中心（数据驱动框架）

- **知识中心** = 字典 + 类型图谱 + 规则库（按 `site.type` 分文件声明式存储）。**读时主动驱动生成**。
- **数据中心** = 案例 = 实例图谱。**写时沉淀 + 离线采矿反哺**。

### 数据中心职责修正（用户定稿）

数据中心**不在实时生成主链路**，只在主链路之外做两件事：
- **作用 1 · 沉淀入库**：生成出来的实例数据，以结果态写进数据中心，积累成案例库。
- **作用 2 · 反哺知识**：需要时从数据中心捞案例，提炼更好的场景规则，回流到知识中心（规则库 / 类型图谱）。

> 这化解了「数据中心是数据还是知识」之争：实例图谱=数据=数据中心；类型图谱=知识=知识中心。

---

## 4. 两阶段 / 两角色

- **知识管理 KM（authoring-time · 离线）**：专家 + LLM 抽取 → 人工校对，建设资产（字典 / 类型 / 规则）。
- **用户使用（per-request · 在线消费）**：NL → 载入类型 + 生成实例 → 引擎。

示例：阿房宫「文献导入」是 KM 动作（双写两中心）；用户「建一座阿房宫」是使用动作。

---

## 5. 三场景统一（差异仅在「类型图谱来源」）

| 场景 | 类型图谱来源 | 说明 |
|---|---|---|
| 四合院 | 专家预写 | MVP 主验证场景 |
| 阿房宫 | 文献抽取 → 校对（双写两中心） | 备选验证场景 |
| 自由想象 | 通用基元 + LLM 现造类型 | 平级占位，不「包含」四合院 |

三层（WHAT/WHERE/HOW）、两层（类型/实例）、两中心恒在；自由想象与四合院为**平级占位**，非包含关系。

---

## 6. 六类场景（验证框架扩展性）

- **提取类**：① 图纸识别 ② 三维设计智能化 ③ 机器狗空间重建
- **生成类**：④ 传统/历史建筑生成（四合院主选 / 阿房宫备选）⑤ 无约束想象生成（我的世界）⑥ 工业产品生成（IFC 跨域）

框架差异仅在「输入路径 / 类型来源 / 领域」，六大场景共用同一骨架。

---

## 7. MVP 范围

**进 MVP：**
- 知识中心：字典 + 四合院类型图谱 + `siheyuan.rules`
- 数据中心：四合院实例图谱（种子一座）
- 几何计算引擎：全量
- 几何造型引擎：BOX 出口
- 渲染层：Three.js 复用 `spawnBoxesFromList`
- 输入/理解层：mock / 混元兜底（NL → 实例图谱）

**砍掉（仅留架构口）：** BREP/OCC、LLM 全景生成器、提取类 ①②③、阿房宫 / 自由想象 / 工业产品场景（架构留口，仅种子四合院一座）。

---

## 8. MVP 六大模块（技术层面）

- **① 输入/理解层**：NL → 实例图谱（MVP 用 mock / 混元兜底）
- **② 知识中心（逻辑容器，非独立运行模块）**：字典 + 类型图谱 + 规则库
- **③ 数据中心（逻辑容器）**：实例图谱
- **④ 几何计算引擎**：吃 ③（实例图谱）+ ②（类型图谱 + 规则库）→ 相对位置清单（非形状，与体素无关）
- **⑤ 几何造型引擎 · BOX 出口**：位置清单 → 体素 box 清单
- **⑥ 渲染层**：box 清单 → Three.js 体素可视化（复用 `spawnBoxesFromList`）

> ② 知识中心、③ 数据中心是**逻辑容器**，不是独立运行的服务，它们给 ④ 提供输入契约。

---

## 9. 实时生成链路（修正后）

1. 用户输入 NL → **① 理解层**
2. **① 直接基于 ② 知识中心** 生成 实例数据（实例图谱）
3. 实例数据 → **④ 几何计算引擎** → 相对位置清单
4. 位置清单 → **⑤ 几何造型引擎（BOX）** → box 清单
5. box 清单 → **⑥ 渲染层** → 体素

数据中心 ③ 在主链路**之外**：沉淀入库 + 反哺知识。

---

## 10. 零坐标红线

- 图谱禁写 x/y/z；方位归规则库；绝对坐标仅由引擎末端生成。
- 此前 `MVP实施文档.md` §4 的 schema v0（节点含 `pos{x,y,z}` / `axisAlign` / Edge `axisAlign`）违反红线，已被本框架取代，待修订。

---

## 11. 核心理念（旧对话 64aa6829 · 2026-09-08）

- **LLM 原生**：大模型为中枢。
- **数据驱动**：数据 / 知识是一等资产，代码其次。
- **「眼-脑-手-人」框架**（角色映射，2026-09-13 修正）：
  - **眼 = 外界采集 / 信息采集**：输入感知层（用户 NL 输入，未来图像 / 扫描入口），把外部信息喂给脑。**≠ 知识中心**。
  - **脑 = 数据中心 + 知识中心 + 大模型 三者融合**：知识中心（dict / 类型图谱 / rules）+ 数据中心（实例种子）+ ① 理解层（LLM）。LLM 只是脑的「推理核」，知识 / 数据是脑的记忆与常识——**LLM ≠ 脑本身**。
  - **手 = 几何计算引擎 + 几何造型引擎**：④几何计算 + ⑤体素 BOX 造型，把脑的输出落成体素。
  - **人 = 用户 / 运营者的介入与反馈（暂定，待拍板）**：驱动雪球越滚越大 + 关键纠偏 / 中途改方案 / 越权暂停。**此角色定义待用户最终确认**。
- Step0 → Step3 节奏纪律。
- 张雪机车打法：**死磕核心 → 枢纽 → 可衍生**（核心做出来是枢纽，场景无限衍生）。

---

## 12. 实施下一步

第一批落地资产文件（知识中心 + 数据中心，纯 JSON/文本，不动引擎代码，作为 ④ 几何计算引擎的输入契约）：
- 命名字典（dictionary）
- 四合院类型图谱（type graph）
- `siheyuan.rules`（规则库）
- 一份四合院实例图谱（instance graph，种子案例）

后续待办（不阻塞实施）：
- 修订 `MVP实施文档.md` §4，使其与本存档零坐标红线一致。

---

## 13. MVP 大模型原生机制（设计锁定）

> 本节基于本次对齐锁定两项前期未决：① MVP 范围限定**北京四合院**（变体仅限四合院家族内，如进数 / 跨院，不扩展其他建筑类型）；② 命名字典补 `进`（院落进数·变体开关）+ `序列`（进沿中轴南北次序）。

### 13.1 定位：LLM 是中枢，不是外挂接口

| | 外挂接口（要避免） | LLM 原生（目标） |
|---|---|---|
| LLM 位置 | 旁路翻译器：硬编码解析 → 调一下 LLM 兜底 | **中枢**：吃知识中心 → 直接产出实例图谱 |
| 知识来源 | prompt 里写死几句 | **知识中心（字典/类型/规则）即 LLM 的上下文**，随资产更新自动变聪明 |
| 输出 | 自由文本，再正则抽取 | **受类型图谱 schema 约束的结构化输出**（函数调用 / structured output），不瞎编图谱 |
| 确定性计算 | 也塞给 LLM 猜 | **全部留在引擎代码**（几何计算/造型/渲染/规则执行），LLM 只做模糊 NL→结构的映射 |

一句话：LLM 是「脑」的推理核与输入理解 / 驱动，④⑤ 几何引擎是「手」——即 §11「眼-脑-手-人」框架里 LLM 为脑的推理核、引擎为手。注意「脑」= 知识中心 + 数据中心 + 大模型融合，LLM 只是脑的一部分，不是脑本身。

### 13.2 MVP 的 LLM 机制（具体链路）

- **模型**：腾讯混元 `hy3-preview`，走 CloudBase OpenAI 兼容网关（已验证可用，全腾讯收敛）。
- **链路**：用户 NL（如「帮我建三进四合院」）→ ① 理解层把**知识中心（dict 语义键 + 类型图谱结构 + 规则）作为上下文**喂给混元 → 混元**输出符合类型图谱 schema 的实例图谱 JSON**（含 `进数=3`、各房角色 / 组合 / 比例模数）→ ④⑤⑥ 确定性引擎接管。
- **字典的关键作用**：dict 的 `key`（语义键）即 LLM 的**唯一合法词表**，只能引用这些键 → 从根上杜绝幻觉出非法节点。

### 13.3 MCP 判定：MVP 不用，Step3 再上

- MVP 1.0 **不引入 MCP**。知识中心、几何引擎均为同项目、进程内资源，直接函数调用即可；MCP 是「把能力暴露给外部 LLM / 客户端」的协议，现在用属 premature。
- MCP 属长期愿景（工程数据发动机 = 统一数据底座）的 **Step3 暴露层**：届时把引擎包成 MCP Server，供外部 Agent / 模型消费——是对外赋能，非 MVP 对内搭建。

### 13.4 科学用 LLM 的 4 条铁律

1. LLM 只干「NL → 受约束的实例图谱」，其余确定性活（几何计算 / 造型 / 渲染 / 规则执行）不给它。
2. 知识中心是 LLM 的活上下文，资产越厚它越聪明（数据驱动闭环）。
3. 输出强约束到类型图谱 schema，dict 键为词表——零幻觉。
4. 流式 SSE 把「机器脑在搭房子」过程可视化（直击动机二：让人看见数据智能在干活）。

### 13.5 与 §11 核心理念对齐

LLM 原生 + 数据驱动 + 「眼-脑-手-人」框架（角色映射见 §11）：脑 = 知识中心 + 数据中心 + 大模型融合（LLM 为推理核，非脑本身），手 = ④⑤ 几何引擎，眼 = 外部信息采集，人 = 用户介入与反馈（暂定）。Step0 → Step3 节奏纪律不变。

**模型策略锁定（2026-09-13 修正）**：MVP 阶段 = **全腾讯栈**（混元 `hy3-preview` + CloudBase 全套），不追 GPT-6、不为极致换模型。GPT-6 等前沿模型仅作「能力上限前瞻预期」（见对话调研），非 MVP 实施项；架构保持模型可插拔，将来随混元 / 腾讯生态模型升级即插即强。
