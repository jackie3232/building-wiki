# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# 系统
.DS_Store
Thumbs.db

# 调试截图（本地产物，不入库）
shot-*.png
shot.png

# 建模引擎运行时产物（每次跑 main.exe 重新生成，不入库）
server/data/shapes_case6.json
server/data/shapes_case6.stl
server/data/*.stl

# server/data 下的临时产物（约定：_ 前缀 = 调试/核验产物、上传的临时图谱等，不入库）
# 该目录需入库的只有 instances/ 子目录（不以 _ 开头，故不受本条影响）
server/data/_*

# 运行日志
server/data/*.log
server/*.log
*.log

# 工作记忆（本地备忘，不入库）
.workbuddy/

# WorkBuddy 应用描述文件（本地工具产物，不入库）
.wbapp_*.genie
