# 工程数据发动机 · MVP 实施文档（暂定名）

> 本文整理 MVP 的「前因后果」与「实施初步方案」，供立项参考。所有结论经多轮讨论 + 行业调研校准；分「观察 / 推断」两档，标注未证实项。
> 命名「工程数据发动机」为用户口述暂定，后边再商定，非定稿；不叫"空间智能"（空间智能只是子集）。

---

## 一、前因后果（为什么做这件事）

### 1. 三个动机（平行，不分先后）
- **动机一 · 深耕底层数据资产**：行业数字化做到顶，最底层"数据"才是最重要资产，但一直没作为长期事深耕——这是最早、也最遗憾的一点。
- **动机二 · 证明"数据重要"**：大模型火了，一直强调数据重要却说服不了人——因为别人没概念、没看到、也证明不了。需要一个能让人**亲眼看到"数据值钱"**的载体。
- **动机三 · 价值链接（策略）**：做开源项目是让更多人知道核心专长，按节奏引外部人进入深耕，可能投资/合作。第1步哪怕是概念/理念让人"看见"就行，再一步步引入，进而投资合作。

### 2. 理念锚：张雪机车打法
- 死磕核心 → 核心做出来是枢纽 → 什么都能做。
- 张雪：死磕发动机 → 枢纽 → 全场景车型（地上跑、天上飞、太空用）。
- 我们：死磕工程数据发动机 → 枢纽 → 全场景数据（工程/建筑/城市，乃至月球/火星家园）。
- 升级：地球上的发动机造好 → 能造月球发动机；地球上的数据做好 → 能做月球/火星数据。**核心枢纽不变，场景无限衍生**。

### 3. 行业调研校准（核心价值怎么来的，含反证）
支持判断的证据：
- Bluebeam 2025：现有 AI「更像第二双眼睛」，且视觉模型在空间推理上仍弱——印证"有眼无脑"。
- 中科院 2026.8：点云重建关键在语义分割，让「几何副本」升级为「可理解的空间数据库」——和你说的"不知墙门窗"吻合。
- Egnyte AEC Summit 2026：行业「淹没在数据里、渴求洞察」——是你动机二的第三方背书。
- MCP 协议 2026 月下载破 9700 万次；Agentic BIM 依赖「project data graph / unified data layer」——技术选型一致。

反证（校准点，让定位更锋利）：
- 行业**不缺单点 AI**（品茗晓筑、广联达 AecGPT、Bentley Copilot、Autodesk、Revit MCP 等），但它们全绑定厂商、闭源、碎片化。
- 所以你不是"补大脑第一人"，而是做那颗**被所有单点共享的「发动机 / 统一数据底座」**的人——真空位更清晰。
- 术语对齐：对外用「数据智能 = data-centric intelligence」接行业话语；"空间智能"只是数据智能的子集。

### 4. 核心价值分两层（用户确认）
- **对外（投资人 / 伙伴）**：张雪式「死磕核心、枢纽化、可衍生」 + 一个能跑的 demo 让人看见数据变聪明。
- **对内（技术 / 团队）**：permissive 开源统一数据智能底座，补行业缺的「机器脑」（理解 + 推理）。

### 5. 第1步标准（验收准则）
- 让**无技术背景、却掌握核心资源**的人一眼看到价值（这类人往往是决策层 / 资源方，对内技术价值他们无感）。
- 场景 / 形态选型 = 最快、最小成本体现价值；验收 = 非技术人看懂「数据智能在干活」，而非渲染美。

### 6. 六类场景（发散讨论：提取 3 + 生成 3）
- ① 图纸识别（提取）：现有方案有机器眼无机器脑，缺语义理解。
- ② 三维设计智能化（提取）：手靠机器、脑子靠人 → 要脑子也靠机器，人退需求方。
- ③ 机器狗空间重建（提取）：点云有几何无语义，需推理补全（看一点推全貌）。
- ④ 传统 / 历史建筑生成（生成 · 有约束）：四合院主选、阿房宫备选；从来源语义还原。
- ⑤ 无约束想象空间生成（生成 · 自由）：我的世界 / 游戏娱乐；创造力面。
- ⑥ 工业产品生成（生成 · 自由）：IFC 跨域（玩具 / 发动机），数据定义驱动强创造力；工业 schema 略异但理念同。
- 三场景同一卡点：有感知 / 执行、缺机器脑 → 发动机补「大脑」。生成类补上「无中生有」，机器脑覆盖理解 + 创造两维。

---

## 二、实施初步方案（怎么做）

### 1. 整体定位
- 对外称 **MVP**，实质 = 最小可用演示 demo（非纯 POC、非重 MVP）。
- 不为上线产品，为「看见价值 + 链接资源」；验收边界 = 核心三件（见下）。

### 2. Step1 具体目标（末轮收窄）
- **极速版 · 北京四合院（体素 Box 版）**，单壳先跑通（我的世界降为后续衍生壳）。
- Box 版 = 1 个院落根节点 + N 个体素 / 房间子节点，靠 contains / axisAlign 表达规制，渲染只画方块。
- 不碰雕刻 / 瓦顶 / 真实材质（那是阿房宫级）。

### 3. MVP 核心三件（验收边界）
- ① 用户最终可见的渲染屏幕：Three.js 体素 3D（可旋转 / 缩放 / 点选看 label）。
- ② 自然语言交互（简单）：底部居中悬浮输入框，输入「三进四合院带跨院」等。
- ③ 后端数据驱动（数据引擎）：后端持「空间关系图谱」→ 生成 box → 推送前端。

### 4. 空间拓扑数据模型 schema v0（已锁）
- 节点 Node：id / type(院落｜房屋｜体块｜廊) / label / size{w,d,h} / pos{x,y,z} / attrs{功能, 朝向, 层级, color}。
- 关系 Edge：contains / adjacent / axisAlign / stacked(留我的世界)。
- color 字段：最简渲染色（正房深灰 / 厢房浅灰 / 廊木色等），非真实材质，成本可忽略、可读性翻倍。
- 双壳映射：四合院 = 院落根 + 房间子节点(contains + axisAlign)；我的世界 = box 节点(stacked)。

### 5. 产品形态与架构
- **UI**：大模型产品式——大展示页 + 底部居中悬浮输入框，下方未来可加示例入口。
- **后端**：空间关系图谱（先建一 / 二 / 三 / 四进标准模板）→ 生成 box → SSE 流式**逐体素推送**前端。
- **流式展示**：一个体素一个体素往前推，把「机器脑在搭房子」过程可视化，比一次性出现冲击力大（直击动机二）。
- **NL**：腾讯混元（Hunyuan）直出 schema；最简关键词映射作兜底。
- **OCC**：Step1 不接，用轻量 box 生成器；OCC 留作接真实 IFC / 复杂几何时的升级替换。
- **生态**：全腾讯收敛——部署 CloudBase、LLM 混元、同账号调用。

### 6. Step1 边界
IN（做）：
- 极简网页：悬浮输入 + 体素 3D 四合院。
- 输入「三进四合院带跨院」→ 混元 → schema v0 → 渲染方块（带渲染色）。
- 典型规制：中轴对称、正房坐北、厢房东西、倒座南、院落嵌套。
- 交互：旋转 / 缩放 / 点选看 label；逐体素流式弹出。
- 部署 building.wiki 根域。

OUT（不做，留后续）：
- 雕刻 / 瓦顶 / 真实材质。
- 我的世界壳（Step2 衍生）。
- 历史精确考据（取典型规制，非某特定四合院复原）。
- 后端存储 / 账号 / 用户系统。
- 提取类（图纸 / 点云 / IFC）、MCP、查询 API、OCC。

### 7. Step1 进一步目标（做完 = 达成什么）
- **价值证明**：资源方输入一句话 → 四合院立起来，直观感到「数据智能在干活」（解动机二）。
- **架构验证**：证明「引擎产 schema + 渲染层分离」成立 → 后续加我的世界壳只需换渲染层（枢纽化第一证）。
- **钩子物料**：对外展示 / 引资的第一件可发的东西。

### 8. 时间估算（推断档 ⚠️ 未证实）
- 最小闭环（渲染 + 规则引擎 + mock NL）：3–5 天。
- + 真实混元接入 + 部署上线：+3–5 天。
- 合计约 **1–2 周**出可对外演示 MVP。
- 实际取决于：后端语言拍板、混元 key 就绪、你部署节奏。

### 9. 长期节奏总览
- **Step0 建壳**：GitHub + README 定位 + MPL-2.0（用户自管项目空间）。
- **Step1 体素四合院 MVP**（本文目标）。
- **Step2 内核 v1**：规范图模型 + 查询 API（进入长期深耕）。
- **Step3 接入层**：LLM 自然语言 → 查询 + MCP 暴露 + 1 个阶段示例（审查 / 算量）。
- 每步都有可演示物。

### 10. 部署与生态
- **域名**：building.wiki 根域，用户自管、可随时部署。
- **平台**：腾讯云开发 CloudBase。
  - 前端静态托管（Three.js 页）。
  - 后端 Python 云函数（空间图谱 → box → SSE 流式）；注意 CloudBase Python 最高 3.10，日后接 OCC 走云托管 Run 容器模式。
  - 自定义域名 CNAME + 免费 SSL；building.wiki 需做备案转入腾讯云。
  - SSE：Flask stream_with_context + text/event-stream，云函数超时调到 60s+。
- **LLM**：腾讯混元，同生态调用。

---

## 三、待办 / 下一步
- 用户自管创建项目空间（本 AI 不建目录）。
- 立项后第一个执行切片：前端悬浮输入 + 后端图谱 + SSE 逐体素（本地开发，部署走 CloudBase）。
- 后端语言：执行时定（OCC 提过 → 偏 Python；提取类也是 Python，统一有利）。
- 混元接入：需用户给 key，本地先用 mock 跑通闭环。
- 纪律红线：未经明确指令不擅自建项目 / 装依赖 / 写码。
