# MVP 开发交接记录（当前状态存档）

> 用途：本对话已收敛 building-wiki 的架构框架并完成存档。**当前聊天即为 MVP 开发团队、已接管**，本存档为权威源，无需重聊背景。
> 配套详细文档：`架构设计总览.md`（框架总存档，本文是其精简交接版）。

---

## 1. 项目一句话定位

building-wiki：图谱驱动的中国古建筑体素 3D 生成（文字/图像 → 图谱 → Box 3D 模型）。
MVP 目标是**验证框架**（以四合院为验证场景，而非以四合院为核心）。

---

## 2. 已锁定的架构（铁律，不可回退）

### 2.1 五大命名（取代早先「BRG 图谱」单体称呼）
- **类型图谱（WHAT·知识）**：构件+角色+组合关系+比例模数（**零坐标、无绝对米数**）
- **实例图谱（数据）**：继承类型结构、填具体长宽进深、可偏离标准型
- **规则库（WHERE）**：方位/朝向/围合约束
- **几何计算引擎（怎么算）**：吃 类型图谱+实例+规则 → **相对位置清单**（纯业务几何计算，只算位置，**与体素无关**）
- **几何造型引擎（新）**：吃位置清单 → 最终几何，两个出口：**BOX 体素（MVP）** / **BREP 真实几何（OCC，延后）**

### 2.2 两层模型
- 类型图谱 = class（参数化模板）；实例图谱 = instance（套具体数据）。类比 CAD 参数化模型 vs 实例化模型。

### 2.3 两中心（数据驱动框架）
- **知识中心** = 字典 + 类型图谱 + 规则库（按 `site.type` 分文件）。**读时主动驱动生成**。
- **数据中心** = 案例 = 实例图谱。**写时沉淀 + 离线反哺**。

### 2.4 实时生成链路（修正后）
1. 用户输入 NL → ① 理解层
2. ① 直接基于 ② 知识中心 生成 实例数据（实例图谱）
3. 实例数据 → ④ 几何计算引擎 → 相对位置清单
4. 位置清单 → ⑤ 几何造型引擎（BOX）→ box 清单
5. box 清单 → ⑥ 渲染层 → 体素

**数据中心在主链路之外**：沉淀入库 + 反哺知识，不参与实时生成。

### 2.5 零坐标红线
- 图谱/类型/实例**禁写 x/y/z**；方位归规则库；绝对坐标仅由几何计算引擎末端生成。

### 2.6 反复纠错沉淀的硬结论（新团队务必遵守）
1. 图谱必须区分**类型图谱 vs 实例图谱**，不得用单体「BRG 图谱」笼统称呼。
2. **几何计算引擎与体素无任何关系**：它只算纯业务几何（相对位置/布局）；BOX/BREP 是几何**造型**引擎的两种产出。
3. 类型图谱无绝对米数、无方位；尺寸只在实例图谱填。
4. 数据中心不在实时生成主链路；只做沉淀 + 反哺知识。

---

## 3. 当前进度

| 项 | 状态 |
|---|---|
| 架构框架多轮讨论收敛 | ✅ 完成 |
| 存档 `架构设计总览.md`（按最新修订，无「BRG 图谱」残留） | ✅ 完成 |
| 第一批资产文件（字典/类型图谱/rules/实例图谱） | ⏳ 待实施 |
| 修订 `MVP实施文档.md` §4 到零坐标红线 | ⏳ 待办（不阻塞实施） |

---

## 4. 下一步实施：第一批资产文件（知识中心 + 数据中心）

> 纯 JSON/文本，**不动引擎代码**，作为 ④ 几何计算引擎的输入契约。

1. **命名字典 `dict.json`** —— 最基础命名（中文 ↔ 稳定语义键），供图谱/规则引用
2. **四合院类型图谱 `siheyuan.type.json`** —— 结构骨架+角色+组合+比例模数（零坐标、无米数）
3. **规则库 `siheyuan.rules`** —— 方位/朝向/围合约束（WHERE）
4. **四合院实例图谱 `siheyuan.instance.json`** —— 种子案例，填具体长宽进深

### 4.1 命名字典草案（已拟，待拍板）
- 结构：`term`(中文) / `key`(语义键 snake_case) / `cat`(类别) / `alias`(别名)
- 5 类根词（最基本粒度，不展开二级）：
  - 空间角色：正房/厢房/倒座房/后罩房/耳房/庭院/垂花门/游廊/影壁
  - 构件：屋顶/墙体/台基/柱/梁/门/窗
  - 方位：东/南/西/北/坐北朝南
  - 尺度模数：间/面阔/进深/明间/次间/梢间
  - 关系：围合/中轴/对称/居中

---

## 5. 两点已拍板（原「本对话未决，留给新团队」——新团队已接手并定稿）

1. **命名字典范围**（已定·方案 A）：5 类根词基础上补 `进`（院落进数·变体开关）+ `序列`（进沿中轴南北次序）；其余维持原草案。粒度够「最基本」，可进 `dict.json`。
2. **字典服务范围**（已定）：MVP 范围限定**北京四合院**，字典只服务四合院（`site.type=siheyuan`），不提前预留多类型字段；变体仅限四合院家族内（进数 / 跨院），框架靠类型图谱参数化预留扩展口。

---

## 6. 关键文件索引

- `架构设计总览.md` —— 框架总存档（本交接的详版）
- `MVP实施文档.md` —— 旧文档，§4 schema v0 违反零坐标红线，待同步修订
- `server/` —— 现有服务端（app.py / engine C++ / static Three.js），几何造型引擎 BOX 出口与渲染层拟复用其 `spawnBoxesFromList`
- （待创建）资产目录：字典/类型图谱/rules/实例图谱

---

## 7. 给 MVP 开发团队的协作纪律

- 小步快跑，一次一个问题；改动前先读实码，零漂移。
- 任何「根因/结论」出口前必须握证据（日志/代码/数据），不凭印象。
- 不擅自代提交 git、不擅自部署云函数/推送预览（除非用户逐条显式指令）。
- 不主动写工作日志/记忆文件（除非用户明确要求记录）。
